{
  "name": "travel-slideshow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "http-server -c-1 -p 3000 .",
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html about.html style.css scripts.js dist/",
    "start": "http-server -c-1 -p 3000 dist"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
